# config/repositories.yml
#
# KPort overlay/repository registry.
# Analogous to Portage's repos.conf / eselect repository.
#
# Overlays are searched in priority order (highest first) before the
# main KPort tree (priority 0). A package found in a higher-priority
# overlay shadows the main tree version.
#
# Fields:
#   name:        unique identifier
#   description: human-readable description
#   url:         git URL to clone/pull the overlay from
#   priority:    integer, higher = checked first (main tree = 0)
#   enabled:     false to disable without removing
#   auto_sync:   true to sync on every `kport sync`

repositories:

  # Main KPort tree — always present, not listed here
  # (implicit, priority 0)

  # Bundled example overlay — demonstrates overlay shadowing.
  # Shadows kf6-karchive with an annotated copy. Safe to disable.
  - name: example
    description: "Bundled example overlay for testing overlay resolution"
    url: ""
    priority: 10
    enabled: true
    auto_sync: false

  # Community 32-bit overlay — lightweight packages for i686 systems.
  # Qt 6 / KDE Frameworks 6 are 64-bit only; this overlay targets GTK/SDL/
  # toolkit-free utilities that still build on 32-bit x86.
  # Enable by setting enabled: true, or point url at a hosted fork.
  - name: community-32bit
    description: "Lightweight i686 (32-bit x86) packages"
    url: ""
    priority: 15
    enabled: false
    auto_sync: false

  # KDE Plasma 5.27 LTS for i686. Requires community-32bit (priority 15)
  # to be enabled alongside it for Qt5/KF5 base libraries.
  # Validated against Arch Linux 32 extra repository (Plasma 5.27.7).
  - name: community-32bit-plasma5
    description: "KDE Plasma 5.27 LTS for i686 (32-bit x86)"
    url: ""
    priority: 20
    enabled: false
    auto_sync: false

  # Example community overlay:
  # - name: kde-git-builds
  #   description: "Bleeding-edge KDE git snapshot builds"
  #   url: https://github.com/Interested-Deving-1896/kport-kde-git
  #   priority: 10
  #   enabled: false
  #   auto_sync: true

  # Example hardware-specific overlay:
  # - name: steamdeck
  #   description: "Steam Deck optimised KDE packages"
  #   url: https://github.com/Interested-Deving-1896/kport-steamdeck
  #   priority: 20
  #   enabled: false
  #   auto_sync: true